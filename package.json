{
  "name": "keyrail",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-aware project identity and credential routing for local development.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "keyrail": "packages/cli/bin/keyrail.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check packages/cli/src/cli.js && node --test",
    "pack:dry-run": "npm pack --dry-run --workspace @keyrail/cli",
    "keyrail": "node packages/cli/bin/keyrail.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "cli",
    "agent",
    "secrets",
    "identity",
    "policy"
  ],
  "license": "MIT"
}
